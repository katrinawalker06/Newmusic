@model Newmusic.Models.BandMemberBand

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>BandMemberBand</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BandMemberBandId" />
            <div class="form-group">
                <label asp-for="BandMemberId" class="control-label"></label>
                <select asp-for="BandMemberId" class="form-control" asp-items="ViewBag.BandMemberId"></select>
                <span asp-validation-for="BandMemberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BandId" class="control-label"></label>
                <select asp-for="BandId" class="form-control" asp-items="ViewBag.BandId"></select>
                <span asp-validation-for="BandId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
